name: Hello
on:
  push:
    branches:
      - main
workflow_dispatch:

jobs:
hello:
runs-on: ubuntu-latest
steps:
- uses: actions/checkout@v2
- name: hello_world
run: echo "Welcome to GitHub Actions"

goodbye:
runs-on: ubuntu-latest
steps:
- name: goodbye_world
run: echo "Good_Bye"
