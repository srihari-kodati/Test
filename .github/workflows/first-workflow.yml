name: F W
on:
    push

jobs:
    read-file:
        runs-on: windows-latest

        steps:

        - name: Checkout only README.md
          uses: actions/checkout@v6
          with:
            sparse-checkout: |
              README.md
            sparse-checkout-cone-mode: true

        - name: List Files
          run: ls -ltra

        - name: W M
          run: echo "GA W F"

        - name: RF
          run: cat README.md